version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./sql_app.db
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
      - ./sql_app.db:/app/sql_app.db
